---
title: "Type Alias: WorkflowContext"
description: Execution context for workflow event processing.
category: Types
sidebar_label: WorkflowContext
---
[Workflow Core API](../README.mdx) / WorkflowContext

> **WorkflowContext** = `object`

Defined in: [context.ts:90](https://github.com/run-llama/llama-flow/blob/33aca370356eefe2f82d1ad3bc7aa2fbb48a0277/packages/core/src/core/context.ts#L90)

Execution context for workflow event processing.

The workflow context provides the runtime environment for executing handlers
and managing event flow. It offers access to the event stream, abort signals,
and methods for sending events within the workflow.

## Example

```typescript
// Get the current context (inside a handler)
const context = getContext();

// Send events
context.sendEvent(
  ProcessEvent.with({ step: 'validation' }),
  LogEvent.with({ message: 'Processing started' })
);

// Access the event stream
const stream = context.stream;
await stream.filter(CompletionEvent).take(1).toArray();

// Check for cancellation
if (context.signal.aborted) {
  throw new Error('Operation cancelled');
}
```

## Accessors

### signal

#### Get Signature

> **get** **signal**(): `AbortSignal`

Defined in: [context.ts:101](https://github.com/run-llama/llama-flow/blob/33aca370356eefe2f82d1ad3bc7aa2fbb48a0277/packages/core/src/core/context.ts#L101)

Abort signal that indicates if the workflow has been cancelled.
Handlers should check this periodically for long-running operations.

##### Returns

`AbortSignal`

***

### stream

#### Get Signature

> **get** **stream**(): [`WorkflowStream`](../classes/WorkflowStream.mdx)\<[`WorkflowEventData`](WorkflowEventData.mdx)\<`any`\>\>

Defined in: [context.ts:95](https://github.com/run-llama/llama-flow/blob/33aca370356eefe2f82d1ad3bc7aa2fbb48a0277/packages/core/src/core/context.ts#L95)

Stream of all events flowing through this workflow context.
Can be used to listen for specific events or create reactive processing chains.

##### Returns

[`WorkflowStream`](../classes/WorkflowStream.mdx)\<[`WorkflowEventData`](WorkflowEventData.mdx)\<`any`\>\>

## Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| <a id="sendevent"></a> `sendEvent` | (...`events`) => `void` | Sends one or more events into the workflow for processing. Events will be delivered to all matching handlers asynchronously. | [context.ts:109](https://github.com/run-llama/llama-flow/blob/33aca370356eefe2f82d1ad3bc7aa2fbb48a0277/packages/core/src/core/context.ts#L109) |
